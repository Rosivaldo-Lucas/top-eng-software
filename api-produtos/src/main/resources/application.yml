spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/db-produtos
    username: db-produtos
    password: root
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: create-drop